# R2dbc props
spring.r2dbc.username=test
spring.r2dbc.password=test
spring.r2dbc.url=${DATABASE_URL}
# Flyway
spring.flyway.user=test
spring.flyway.password=test
spring.flyway.url=${DATABASE_MIGRATION_CONNECTION_URL}